The easiest test to write was the one for ensuring that the model keeps defaults if there isn't any fake data. This is due to the model doing this automatically if nothing is passed to it. I found a lot of the widgets difficult to test. The worst one was testing navigation. I would probably change the design because I had to force a small screen size to correctly test the page opening on tap.